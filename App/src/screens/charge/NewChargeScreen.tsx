import React, { FunctionComponent } from 'react';
import { StyleSheet, Text, View } from 'react-native';

const NewChargeScreen: FunctionComponent = () => {
	return (
		<View>
			<Text></Text>
		</View>
	);
};

export default NewChargeScreen;

const styles = StyleSheet.create({});
